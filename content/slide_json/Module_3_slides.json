{
  "metadata": {
    "title": "Chapter 3: EKS Deployment Pipelines",
    "module_number": 3,
    "course_name": "IO-107: SDLC Pipeline and Deployment Guardrails",
    "duration_minutes": 40,
    "total_modules": 8,
    "template": "roi_syf"
  },
  "slides": [
    {
      "layout": "title",
      "subtitle": "Chapter 3:\nEKS Deployment Pipelines",
      "title": "IO-107: SDLC Pipeline and Deployment Guardrails"
    },
    {
      "layout": "objectives",
      "title": "Chapter Objectives",
      "bullets": [
        "In this chapter, you will:",
        "Deploy applications to EKS using Helm charts via CodeBuild pipelines",
        "Deploy applications using kubectl apply and Kustomize via CodeBuild",
        "Configure IRSA (IAM Roles for Service Accounts) for secure pod-to-AWS access",
        "Read EKS deployment rollout status and pod health from the pipeline"
      ]
    },
    {
      "layout": "title_only",
      "title": "Chapter Concepts",
      "table": {
        "headers": [],
        "rows": [
          [
            "The EKS Deployment Pipeline"
          ],
          [
            "Helm Chart Deployments via CodeBuild"
          ],
          [
            "kubectl Apply via CodeBuild"
          ],
          [
            "IRSA (IAM Roles for Service Accounts)"
          ],
          [
            "Fargate and Deployment Validation"
          ]
        ]
      },
      "highlight_row": 0
    },
    {
      "layout": "title_only",
      "title": "The EKS Deployment Pipeline",
      "cards": [
        {
          "header": "Build side",
          "header_color": "blue",
          "items": [
            "Source: app code + manifests / Helm chart",
            "Compile, build Docker image, push to ECR",
            "OPA validates manifests (Chapter 6)"
          ]
        },
        {
          "header": "Deploy side",
          "header_color": "green",
          "items": [
            "Push-based: CodeBuild runs helm or kubectl",
            "helm upgrade --install OR kubectl apply",
            "Validate rollout before reporting success"
          ]
        }
      ],
      "speaker_notes": "Push-based deployment is the primary EKS pattern: the pipeline actively deploys to the cluster — CodeBuild runs helm upgrade or kubectl apply, and the deployment happens. The pipeline is in control, which integrates well with our CodePipeline infrastructure and approval gates. (Pull-based GitOps with ArgoCD or Flux exists and we are evaluating it for future use, but push-based is what you will see today.) The pipeline shape is the standard one: source pulls the application code and Kubernetes manifests, build compiles the app and pushes a Docker image to ECR, OPA validates the manifests before any deployment runs (we cover this in Chapter 6), and the deploy stage runs helm or kubectl against the target cluster. We'll cover Helm first, then kubectl, then permissions via IRSA, and finally Fargate plus deployment validation."
    },
    {
      "layout": "title_only",
      "title": "Chapter Concepts",
      "table": {
        "headers": [],
        "rows": [
          [
            "The EKS Deployment Pipeline"
          ],
          [
            "Helm Chart Deployments via CodeBuild"
          ],
          [
            "kubectl Apply via CodeBuild"
          ],
          [
            "IRSA (IAM Roles for Service Accounts)"
          ],
          [
            "Fargate and Deployment Validation"
          ]
        ]
      },
      "highlight_row": 1
    },
    {
      "layout": "content",
      "title": "What is Helm?",
      "bullets": [
        "Package manager for Kubernetes (like apt, yum, or npm)",
        "Charts: templated Kubernetes manifests + a values file",
        "Values: per-environment configuration (replicas, image tag, IAM role)",
        "Releases: deployed instances with revision history and rollback",
        "Shared charts are stored in ECR (Amazon ECR supports OCI Helm artifacts)"
      ],
      "speaker_notes": "A Helm chart is a collection of Kubernetes manifest templates plus a values file. The templates have placeholders — image tag, replica count, resource limits — and the values file fills them in for a specific deployment. When you deploy a chart, Helm creates a release that tracks what was deployed, so you can upgrade, roll back, or delete it as a unit. That release management is Helm's key value over raw kubectl. On storage: Amazon ECR supports OCI Helm chart artifacts, which is the standard for shared charts that multiple teams use. Application-specific charts typically live in the same Git repository as the application code, which keeps the chart and the code that uses it versioned together."
    },
    {
      "layout": "code",
      "title": "values.yaml for Environment Configuration",
      "code": "# values-dev.yaml\nreplicaCount: 1\nimage:\n  repository: 123456789012.dkr.ecr.us-east-1.amazonaws.com/myapp\n  tag: \"1.0.0\"\nresources:\n  limits:    { cpu: 500m, memory: 512Mi }\n  requests:  { cpu: 100m, memory: 128Mi }\nenvironment: dev\nserviceAccount:\n  annotations:\n    eks.amazonaws.com/role-arn: arn:aws:iam::<acct>:role/app-dev",
      "speaker_notes": "One values file per environment. This is the dev one — smaller replica count, lower resource limits, and a dev-specific IAM role for IRSA (we'll get to IRSA in a few slides). Staging and production would have their own files with more replicas, higher resource limits, and different IAM roles. The pipeline picks the right file based on the target environment, so the same chart deploys differently to dev, stg, and prd without any template duplication."
    },
    {
      "layout": "code",
      "title": "Helm Commands in CodeBuild",
      "code": "# buildspec.yml — install phase\nphases:\n  install:\n    commands:\n      - curl -L \\\n          https://raw.githubusercontent.com/helm/\\\n            helm/main/scripts/get-helm-3 \\\n          | bash\n      - aws eks update-kubeconfig --name $CLUSTER_NAME \\\n        --region $AWS_REGION",
      "speaker_notes": "Install phase: install Helm and configure kubectl using aws eks update-kubeconfig — this uses CodeBuild's IAM role credentials. Production caveat on the install line: piping curl to bash from a public GitHub URL is a teaching shortcut. Real CodeBuild images pin a specific Helm version, pull from an internal mirror, or bake Helm directly into the build image — never trust a public raw GitHub URL at runtime. Next slide shows the build phase with the actual helm upgrade command."
    },
    {
      "layout": "code",
      "title": "Helm Commands in CodeBuild (continued)",
      "code": "# buildspec.yml — build phase\n  build:\n    commands:\n      - helm upgrade --install myapp ./charts/myapp \\\n          --namespace $NAMESPACE \\\n          --values ./charts/myapp/values-$ENVIRONMENT.yaml \\\n          --set image.tag=$IMAGE_TAG \\\n          --atomic \\\n          --timeout 10m",
      "speaker_notes": "Build phase: helm upgrade --install is idempotent (installs if the release does not exist, upgrades if it does). We pass the per-environment values file and override the image tag with the one we built earlier in the pipeline. Two flags matter most. --atomic is the rollback flag — if pods do not become healthy, Helm automatically rolls back. --timeout caps the wait so the build does not hang forever."
    },
    {
      "layout": "title_only",
      "title": "Rollback Strategies",
      "cards": [
        {
          "header": "Automatic",
          "header_color": "blue",
          "items": [
            "--atomic flag triggers rollback on failure",
            "Required in all production pipelines",
            "No manual intervention needed"
          ]
        },
        {
          "header": "Manual",
          "header_color": "green",
          "items": [
            "helm rollback <release> <revision>",
            "View history: helm history myapp",
            "Pipeline-triggered on detected failure"
          ]
        }
      ],
      "speaker_notes": "With --atomic, Helm handles rollback automatically: if pods do not become healthy, the release reverts. That is required in all production pipelines — a failed deployment must auto-revert, not leave the cluster in a broken state. For manual rollback, helm rollback myapp 2 reverts to revision 2, and helm history myapp shows you the revision list. Manual rollback is rare in practice because --atomic catches most failures during the deploy itself; it is mainly used when an issue surfaces after the deploy finished apparently-successfully."
    },
    {
      "layout": "title_only",
      "title": "Chapter Concepts",
      "table": {
        "headers": [],
        "rows": [
          [
            "The EKS Deployment Pipeline"
          ],
          [
            "Helm Chart Deployments via CodeBuild"
          ],
          [
            "kubectl Apply via CodeBuild"
          ],
          [
            "IRSA (IAM Roles for Service Accounts)"
          ],
          [
            "Fargate and Deployment Validation"
          ]
        ]
      },
      "highlight_row": 2
    },
    {
      "layout": "title_only",
      "title": "kubectl Apply with Kustomize",
      "callout_boxes": [
        {
          "header": "kubectl apply",
          "bullets": [
            "Direct deploy — no charts, no releases",
            "Simpler than Helm for straightforward apps",
            "Good for teams new to Kubernetes"
          ],
          "position": "left"
        },
        {
          "header": "Kustomize overlays",
          "bullets": [
            "base/ holds shared manifests",
            "overlays/dev|stg|prd/ patch the base",
            "Patches namespace, replicas, image tag"
          ],
          "position": "right"
        }
      ],
      "speaker_notes": "kubectl apply takes YAML manifests and applies them to the cluster — no charts, no releases, just manifests. For simple applications or teams new to Kubernetes, this can be easier to understand and troubleshoot than Helm. The environment-configuration problem (different replicas, namespaces, image tags per environment) is solved by Kustomize: you have a base directory with shared manifests, and overlay directories for dev, stg, and prd that patch the base without duplicating it. An overlay might change the namespace to myteam-prd, set replicas to 5, and override the image tag. Kustomize merges all of that at deployment time."
    },
    {
      "layout": "code",
      "title": "kubectl Commands in CodeBuild",
      "code": "# buildspec.yml\nphases:\n  install:\n    commands:\n      - aws eks update-kubeconfig --name $CLUSTER_NAME \\\n        --region $AWS_REGION\n  build:\n    commands:\n      - kubectl apply -k manifests/overlays/$ENVIRONMENT \\\n        --dry-run=server\n      - kubectl apply -k manifests/overlays/$ENVIRONMENT\n      - kubectl rollout status deployment/myapp -n $NAMESPACE \\\n        --timeout=5m",
      "speaker_notes": "Configure kubeconfig, then run kubectl apply with -k for Kustomize. Critical line: --dry-run=server before the real apply. Server-side dry-run sends the request to Kubernetes without creating resources and validates it against the actual cluster state, admission webhooks, and policies. That catches errors the client-side dry-run would miss — for example, a manifest that is syntactically valid but violates a Pod Security policy. Always use server-side dry-run in pipelines. After the real apply, kubectl rollout status waits for the deployment to complete and reports success or failure. If rollout status fails or times out, the pipeline fails."
    },
    {
      "layout": "title_only",
      "title": "Chapter Concepts",
      "table": {
        "headers": [],
        "rows": [
          [
            "The EKS Deployment Pipeline"
          ],
          [
            "Helm Chart Deployments via CodeBuild"
          ],
          [
            "kubectl Apply via CodeBuild"
          ],
          [
            "IRSA (IAM Roles for Service Accounts)"
          ],
          [
            "Fargate and Deployment Validation"
          ]
        ]
      },
      "highlight_row": 3
    },
    {
      "layout": "content",
      "title": "The IRSA Problem",
      "bullets": [
        "Pods need AWS credentials to call AWS APIs (S3, SNS, Aurora)",
        "Old approach 1: EC2 instance profile — every pod on the node shares permissions",
        "Old approach 2: hardcoded credentials — security incident waiting to happen",
        "IRSA: pod-level IAM roles via Kubernetes service accounts",
        "No credentials stored in the pod; uses short-lived projected tokens"
      ],
      "speaker_notes": "Before IRSA, you had two bad options. Option one: use the EC2 instance profile, which meant every pod on the node had the same permissions — way too broad. Option two: put AWS credentials in environment variables or files, which is a security incident waiting to happen. IRSA — IAM Roles for Service Accounts — solves both by giving each pod its own IAM role with exactly the permissions it needs. No credentials are stored in the pod; the pod receives a short-lived projected token that AWS SDKs automatically exchange for temporary credentials. This is the pattern for every pod that needs to call AWS — no static keys, no shared instance-profile permissions."
    },
    {
      "layout": "content",
      "title": "How IRSA Works",
      "bullets": [
        "EKS cluster registers an OIDC identity provider with IAM",
        "IAM role trust policy scoped to that OIDC provider",
        "Trust condition: specific namespace + service account only",
        "Service account is annotated with the IAM role ARN",
        "IRSA webhook injects projected token; AWS SDKs assume the role"
      ],
      "speaker_notes": "Your EKS cluster has an OIDC identity provider registered with IAM. You create an IAM role that trusts this provider — and critically, the trust policy is scoped to a specific Kubernetes namespace and service account combination, not just 'any pod in this cluster'. You then create a Kubernetes service account and annotate it with the IAM role ARN. When a pod runs with that service account, the IRSA admission webhook injects a projected service-account token into the pod at scheduling time. AWS SDKs detect the token and automatically call sts:AssumeRoleWithWebIdentity to get temporary credentials. Result: tight scoping. Even if someone compromises a different pod in a different namespace, they cannot assume this role — their token will not match the trust policy's namespace/service-account conditions. (We'll show the full trust policy JSON in Lab 1 when you set up IRSA hands-on; for now just remember the model: OIDC provider trust + namespace/SA conditions = pod-scoped permissions.)"
    },
    {
      "layout": "content",
      "title": "IRSA in Pipelines",
      "bullets": [
        "IRSA role provisioned via Terraform in the same pipeline as the app",
        "Terraform creates the IAM role with correct trust policy + namespace/SA scope",
        "Kubernetes manifests reference the role via service account annotation",
        "Cross-account IRSA enables access to shared data lakes and central logging",
        "Cross-account IRSA crosses an audit boundary — extra review required"
      ],
      "speaker_notes": "The IRSA role is provisioned through the same pipeline that deploys the application. Terraform creates the IAM role with the correct trust policy and namespace/service-account scope, and the Kubernetes manifests reference it via the service-account annotation. So the IAM role and the workload that uses it are versioned and reviewed together. Cross-account IRSA follows the same pattern but the IAM role lives in a different account — this is the common pattern for accessing shared data lakes or centralised logging. Important callout: cross-account IRSA crosses an audit boundary. The platform team reviews the trust policy on every new cross-account IRSA role, and both accounts' CloudTrail trails must be wired to the central trail — the cluster's account-A trail records the AssumeRoleWithWebIdentity call, and the resource's account-B trail records the subsequent API actions. Don't treat cross-account IRSA as a frictionless copy of single-account IRSA — it is the same mechanism with more review."
    },
    {
      "layout": "title_only",
      "title": "Chapter Concepts",
      "table": {
        "headers": [],
        "rows": [
          [
            "The EKS Deployment Pipeline"
          ],
          [
            "Helm Chart Deployments via CodeBuild"
          ],
          [
            "kubectl Apply via CodeBuild"
          ],
          [
            "IRSA (IAM Roles for Service Accounts)"
          ],
          [
            "Fargate and Deployment Validation"
          ]
        ]
      },
      "highlight_row": 4
    },
    {
      "layout": "title_only",
      "title": "EKS on Fargate — Benefits and Limits",
      "cards": [
        {
          "header": "Benefits",
          "header_color": "blue",
          "items": [
            "Serverless pods — no EC2 nodes to manage",
            "Per-pod isolation and pricing",
            "Profiles select pods by namespace + labels"
          ]
        },
        {
          "header": "Limitations",
          "header_color": "green",
          "items": [
            "Private subnets only (NAT required)",
            "No daemonsets, privileged, or hostPath",
            "No GPU/Inferentia; 1-min minimum billing"
          ]
        }
      ],
      "speaker_notes": "With Fargate, you do not manage EC2 nodes — AWS handles the compute infrastructure. You define a Fargate profile that selects which pods run on Fargate based on namespace and labels (for example: namespace myteam, label compute=fargate). You get per-pod isolation and pricing, which is great for variable workloads or teams that do not want node-management overhead. The limitations matter: Fargate only works in private subnets, so you need a NAT gateway for internet egress. You cannot run daemonsets, privileged containers, or anything using hostPath volumes. There is no GPU or Inferentia option — accelerator workloads must use EC2 node groups. And Fargate enforces a 1-minute minimum billing per pod, so short-lived pods pay for a full minute even if they exit in seconds. Workloads needing accelerators, hostPath, or sub-minute lifecycles belong on EC2 node groups."
    },
    {
      "layout": "title_only",
      "title": "Reading Deployment Status from the Pipeline",
      "callout_boxes": [
        {
          "header": "kubectl checks",
          "bullets": [
            "rollout status — deployment object completed",
            "get pods — Running, not CrashLoopBackOff",
            "Compare readyReplicas vs spec.replicas"
          ],
          "position": "left"
        },
        {
          "header": "Probes do the real work",
          "bullets": [
            "Readiness — pod gets traffic only when ready",
            "Liveness — restarts unresponsive pods",
            "Both required by OPA policy"
          ],
          "position": "right"
        }
      ],
      "speaker_notes": "The pipeline should not report success just because kubectl returned zero — we need to confirm the deployment actually works. Three checks in order. First, kubectl rollout status with a timeout confirms the Deployment object reached the desired state. Second, kubectl get pods shows actual pod state — Running, Pending, CrashLoopBackOff. Third, compare readyReplicas against spec.replicas; if they do not match, fail the build. Underneath, two probe types do the real work: readiness probes determine when a new pod starts receiving traffic during a rolling update, and liveness probes restart pods that have stopped responding. Without probes, Kubernetes will route traffic to pods that are not yet ready, and your users will see errors during every deployment. Every deployment manifest must define both probes — that is the OPA-enforced expectation we cover in Chapter 6."
    },
    {
      "layout": "summary",
      "title": "Chapter Summary",
      "bullets": [
        "In this chapter, you have:",
        "Deployed to EKS using Helm charts with --atomic rollback via CodeBuild",
        "Deployed with kubectl apply, Kustomize overlays, and server-side dry-run",
        "Configured IRSA for pod-scoped, credential-free access to AWS APIs",
        "Read EKS rollout status, pod health, and probe output from the pipeline"
      ]
    }
  ]
}
